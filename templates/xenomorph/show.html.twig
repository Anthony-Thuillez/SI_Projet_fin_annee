{% extends 'base.html.twig' %}

{% block title %}Xenomorph{% endblock %}

{% block body %}
    <h1>Xenomorph</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ xenomorph.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ xenomorph.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ xenomorph.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ xenomorph.price }}</td>
            </tr>
            <tr>
                <th>Img_src</th>
                <td>{{ xenomorph.imgSrc }}</td>
            </tr>
            <tr>
                <th>Img_alt</th>
                <td>{{ xenomorph.imgAlt }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ xenomorph.size }}</td>
            </tr>
            <tr>
                <th>Feeding</th>
                <td>{{ xenomorph.feeding }}</td>
            </tr>
            <tr>
                <th>Dangerous</th>
                <td>{{ xenomorph.dangerous }}</td>
            </tr>
            <tr>
                <th>Available</th>
                <td>{{ xenomorph.available ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ xenomorph.type }}</td>
            </tr>
            <tr>
                <th>Pressure</th>
                <td>{{ xenomorph.pressure }}</td>
            </tr>
            <tr>
                <th>Temperature</th>
                <td>{{ xenomorph.temperature }}</td>
            </tr>
            <tr>
                <th>Atmosphere</th>
                <td>{{ xenomorph.atmosphere }}</td>
            </tr>
            <tr>
                <th>Atmoshpere_value</th>
                <td>{{ xenomorph.atmoshpereValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('xenomorph_index') }}">back to list</a>

    <a href="{{ path('xenomorph_edit', {'id': xenomorph.id}) }}">edit</a>

    {{ include('xenomorph/_delete_form.html.twig') }}
{% endblock %}